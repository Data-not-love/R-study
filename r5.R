# in giá trị
3 * 4

# chỉ chứa x, ko in
x -> 3 * 4 

# kết hợp nhiều gia trị vào 1 vector
primes <- c (2,3,5,7)
primes * 3 

# phím Alt+– = <-
# hàm
 x <- seq( 1,23 )
 x * 3
 


 ggplot(data = mpg,mapping = aes(x = displ, y = hwy)) +
   geom_point() + 
   geom_smooth(method = "lm")
 
